import random

var = random.randint(0,15)



#変数varが、3の倍数の場合は「FIzz」を入力
if var % 3 == 0:
    print("Fizz")

#変数varが、5の倍数の場合は「Buzz」を入力
if var % 5 == 0:
   print("Buzz")

#変数varが、3の倍数、5の倍数の両方に該当する場合は「FIzzBuzz」を入力
if var % 3 == 0 and var % 5 ==0:
   print("FizzBuzz") 

#上記のどの場合にも該当しない場合は変数varの値を入力
else:
    print(var)


